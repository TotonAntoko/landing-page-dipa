import React from 'react';
import './insight.scss';

const Insight = () => (
  <div className="insights container">
    <p>Ready to work your project? Let’s get to work.</p>
    <button type="submit">Let`s Work Togeter</button>
  </div>
);

export default Insight;
